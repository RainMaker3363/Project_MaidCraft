using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StageManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GameManager.GetInstance.Initilize_GameManager();
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
